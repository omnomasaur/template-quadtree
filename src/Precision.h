//Defines a new type for us to use, so that we can change precision as desired.  

#pragma once

#include <math.h>

namespace qt
{
	typedef float real;

	//real real_sqrt(real value)
	//{
	//	return sqrt(value);
	//}
}